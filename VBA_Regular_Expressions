Function RegexMatches(strInput As String) As String

Dim rMatch As Object
Dim s As String
Dim arrayMatches()
Dim i As Long

With New RegExp
    .Global = True
    .MultiLine = True
    .IgnoreCase = True
    'insert the regex pattern you want here
    .Pattern = "Topic n°\s?\d{5}\d?"
    
    If .Test(strInput) Then
        For Each rMatch In .Execute(strInput)
            ReDim Preserve arrayMatches(i)
            'to avoid errors later in case there are spaces.
            arrayMatches(i) = Replace(rMatch.Value, " ", "")
            i = i + 1
            
        Next
    End If
End With

'returning a list of the regex matches joined with a space

RegexMatches = Join(arrayMatches, " ")

End Function
